-- Création de la base (optionnel : createDatabaseIfNotExist=true le fait déjà via JDBC)
CREATE DATABASE IF NOT EXISTS doctoratdb
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE doctoratdb;
